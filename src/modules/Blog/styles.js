import { makeStyles } from "@material-ui/core/styles";

const blog = makeStyles({
  root: {
    height: "inherit",
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
    alignItems: "stretch",
    justifyContent: "space-evenly",
  },
});

export default blog;
